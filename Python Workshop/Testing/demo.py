# pip install pytest
# automated testing --> write code to do tests, use a command to run ALL tests from a project

def add_hello_string(string):
    new_string = f'hello {string}'
    return new_string

print(add_hello_string('world'))